server:
  host: "0.0.0.0"
  port: 8000

http:
  port: 8090

database:
  dsn: "postgres://utmn_user:utmn_password@localhost:5432/utmn_map?sslmode=disable"

tgbot:
  token: "YOUR_TELEGRAM_BOT_TOKEN"
  developers_chat_id: -1 # Replace with actual chat ID

jwt:
  secret: "your-secret-key-here-replace-in-production"
  expiration_hours: 24
